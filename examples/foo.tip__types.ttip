foo(p,x): μx1.(⭡int,x1) -> int
{
var f: int,q: ⭡int;
if (((*p) == 0)) {
f = 1;
} else {
q = alloc 0;
*q = ((*p) - 1);
f = ((*p) * x(q,x));
}
return f;
}

main(): () -> int
{
var n: int;
n = input;
return foo(&n,foo);
}