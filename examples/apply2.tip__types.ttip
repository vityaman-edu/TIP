apply(f,a): ((int) -> int,int) -> int
{
return f(a);
}

fib(n): (int) -> int
{
var result: int;
if ((n > 1)) {
result = (fib((n - 1)) + fib((n - 2)));
} else {
result = 1;
}
return result;
}

main(): () -> int
{
return apply(fib,5);
}